{
  "name": "lplarecursos",
  "version": "1.0.2",
  "description": "Servidor REST de arquivos simples",
  "keywords": [
    "arquivos"
  ],
  "license": "ISC",
  "author": "LPLA-br",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": " cd ./test && jest --verbose",
    "build": "cd ./src && npm exec tsc",
    "start": "echo \"leia o readme para executar o servidor da porta e ip desejado.\"",
    "clear": "rm -r ./build/* || ls test/ | grep --invert-match '.*.js' | xargs -d '\n' -I {} rm ./test/{}",
    "build:prod": "npm install && npm run build && npm prune --production",
    "start:prod": "if [[ ! -f ./public/client.sh ]]; then cp ./bin/client.sh ./public; fi && npm run start"
  },
  "dependencies": {
    "express": "^5.1.0",
    "express-validator": "^7.2.1",
    "minimist": "^1.2.8",
    "multer": "^1.4.5-lts.2",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/multer": "^1.4.12",
    "@types/node": "^22.14.1",
    "@types/swagger-ui-express": "^4.1.8",
    "typescript": "^5.8.3"
  }
}
